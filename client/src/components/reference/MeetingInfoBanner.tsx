import { RadioTower } from 'lucide-react';

// Completely removed club meetings banner as requested
export default function MeetingInfoBanner() {
  // Return empty fragment - component is kept to avoid breaking imports but doesn't display anything
  return null;
}