workflows:
  ios-app-store:
    name: Ham Radio Exam Canada – iOS App Store
    max_build_duration: 60
    instance_type: mac_mini_m2

    environment:
      groups:
        - ios-credentials
      node: 20.17.0
      xcode: latest
      cocoapods: default

    scripts:
      - name: Install dependencies
        script: npm ci

      - name: Build web app
        script: npm run build

      - name: Sync Capacitor
        script: npx cap sync ios

      - name: Install CocoaPods
        script: |
          cd ios/App
          pod install

      - name: Write API key to file
        script: |
          python3 -c "
          import os, re
          key = os.environ.get('APP_STORE_CONNECT_PRIVATE_KEY', '')
          key = key.replace('\r', '')
          if not key.strip():
              raise SystemExit('ERROR: APP_STORE_CONNECT_PRIVATE_KEY is empty!')
          if key.count('\n') < 3:
              m = re.match(r'(-----BEGIN [^-]+-----)\s*(.*?)\s*(-----END [^-]+-----)', key, re.DOTALL)
              if m:
                  header, body, footer = m.group(1), m.group(2), m.group(3)
                  body = body.replace(' ', '').replace('\n', '')
                  chunks = [body[i:i+64] for i in range(0, len(body), 64)]
                  key = header + '\n' + '\n'.join(chunks) + '\n' + footer + '\n'
          with open('/tmp/asc_key.p8', 'w') as f:
              f.write(key)
          print('Key written successfully. Lines:', key.count('\n'))
          "
          echo "--- Key file preview (first line) ---"
          head -1 /tmp/asc_key.p8
          echo "--- Key file line count ---"
          wc -l /tmp/asc_key.p8

      - name: Set up keychain
        script: keychain initialize

      - name: Fetch signing files from App Store Connect
        script: |
          app-store-connect fetch-signing-files \
            ca.hamradioexam.canada \
            --platform IOS \
            --type IOS_APP_STORE \
            --private-key "$(cat /tmp/asc_key.p8)" \
            --key-id "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            --issuer-id "$APP_STORE_CONNECT_ISSUER_ID" \
            --create
          keychain add-certificates

      - name: Set up code signing settings
        script: xcode-project use-profiles

      - name: Set build number
        script: |
          LATEST=$(app-store-connect get-latest-testflight-build-number 6761153301 \
            --platform IOS \
            --private-key "$(cat /tmp/asc_key.p8)" \
            --key-id "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            --issuer-id "$APP_STORE_CONNECT_ISSUER_ID" \
            2>/dev/null || echo "1")
          NEXT=$((LATEST + 1))
          echo "Setting build number to $NEXT"
          cd ios/App
          xcrun agvtool new-version -all $NEXT

      - name: Build and sign iOS app
        script: |
          xcode-project build-ipa \
            --workspace ios/App/App.xcworkspace \
            --scheme App \
            --config Release

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
        beta_app_description: "Canadian amateur radio license exam prep app with 630 official ISED questions and Morse code training."
        submit_to_app_store: false
